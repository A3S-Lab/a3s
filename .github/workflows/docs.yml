name: Deploy Docs

on:
  pull_request:
    paths:
      - 'apps/docs/**'
      - 'apps/cloud'
      - 'crates/acl'
      - 'crates/runtime'
      - 'compat/cloud-stack.acl'
      - '.github/workflows/docs.yml'
  push:
    branches: [main]
    paths:
      - 'apps/docs/**'
      - 'apps/cloud'
      - 'crates/acl'
      - 'crates/runtime'
      - 'compat/cloud-stack.acl'
      - '.github/workflows/docs.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      DOCS_BASE: /a3s/
      DOCS_ORIGIN: https://a3s-lab.github.io
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Initialize locked Cloud documentation sources
        run: >-
          git -c url.https://github.com/.insteadOf=git@github.com:
          submodule update --init --depth 1 apps/cloud crates/acl crates/runtime

      - uses: dtolnay/rust-toolchain@stable

      - uses: swatinem/rust-cache@v2
        with:
          workspaces: apps/cloud -> target

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/docs/package-lock.json

      - name: Install dependencies
        working-directory: apps/docs
        run: npm ci

      - name: Validate Cloud documentation contracts
        working-directory: apps/docs
        run: npm run check:cloud

      - name: Test website routing
        working-directory: apps/docs
        run: npm run test

      - name: Validate shipped Cloud ACL configurations
        working-directory: apps/cloud
        run: |
          cargo test --locked -p a3s-cloud-control-plane loads_shipped_cloud_acl
          cargo test --locked -p a3s-cloud-node-agent loads_shipped_node_example_acl

      - name: Typecheck website
        working-directory: apps/docs
        run: npm run typecheck

      - name: Build
        working-directory: apps/docs
        run: npm run build

      - name: Validate exported website
        working-directory: apps/docs
        run: npm run check:site

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: apps/docs/out

  deploy:
    if: github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
