`core` owns the canonical Lambda domain model, task state machine, DTOs, and
repository traits. It is the only active sub-crate still used by the shared
[`a3s-lambda`](/Users/roylin/Desktop/code/a3s/crates/lambda/Cargo.toml) crate.
