---
title: Build a Multi-Agent Review Flow
description: Use A3S Code v2.1.0 task delegation, registered agent directories, and run inspection from the Node SDK.
full: true
---

import { Callout } from 'fumadocs-ui/components/callout';

# Build a Multi-Agent Review Flow

This tutorial builds a review flow from the current A3S Code v2.1.0 Node SDK:
one parent session delegates focused work, gathers compact child results, and
uses run replay for inspection.

<Callout type="info">
The snippets below use these Node SDK entry points:
`Agent.create`, `agent.session`, `session.registerAgentDir`,
`session.delegateTask`, `session.parallelTask`, `session.runs`, and
`session.runEvents`.
</Callout>

## What Changed

In v2.1.0, routine multi-agent work is driven through task delegation from a
normal session. Child runs return compact results instead of pushing full
transcripts back into the parent prompt.

```text
review goal
  -> lead session
  -> delegateTask / parallelTask
  -> child summaries
  -> run replay and verification evidence
```

## Agent Config

Use ACL, not legacy HCL:

```text
default_model = "openai/docs-model"

providers "openai" {
  apiKey = env("A3S_OPENAI_API_KEY")
  baseUrl = env("A3S_OPENAI_BASE_URL")

  models "docs-model" {
    name = "Docs Model"
    tool_call = true
  }
}
```

## Optional Role Files

`registerAgentDir()` loads role definitions from a directory. Keep role files
small and operational.

```text
agents/
  release-reviewer.yaml
```

```yaml
name: release-reviewer
description: Reviews release blockers and separates required fixes from follow-up work.
prompt: |
  Inspect the requested scope. Report blockers first, then risks, then optional improvements.
```

## Coordinator

```ts
import { Agent } from '@a3s-lab/code';
import path from 'node:path';

const agent = await Agent.create('agent.acl');
const session = agent.session(process.cwd(), {
  builtinSkills: true,
  planningMode: 'enabled',
});

const registered = session.registerAgentDir(path.join(process.cwd(), 'agents'));
console.log({ registered });

const summary = await session.delegateTask({
  agent: 'general',
  description: 'Review auth release blockers',
  prompt: 'Review src/auth for release blockers. Focus on token validation and error handling.',
  maxSteps: 4,
});

const parallel = await session.parallelTask([
  {
    agent: 'general',
    description: 'Auth security review',
    prompt: 'Review src/auth for security risks.',
    maxSteps: 3,
  },
  {
    agent: 'general',
    description: 'Auth test review',
    prompt: 'Review src/auth tests for missing edge cases.',
    maxSteps: 3,
  },
]);

const final = await session.send(`
Merge these child review results into a findings-first release note.

Single review:
${summary.output}

Parallel reviews:
${parallel.output}
`);

console.log(final.text);
console.log(final.verificationSummaryText);
console.log(await session.runs());
```

## Inspect the Run

Use replay APIs for run evidence:

```ts
const runs = await session.runs();
for (const run of runs) {
  console.log(run.id, run.status);
  console.log(await session.runEvents(run.id));
}
```

`currentRun()` may be `null` when the session is idle. Use `runs()` for completed
history and inspect a run's status before attempting cancellation.

## Production Boundary

Direct host calls such as `session.bash()`, `session.tool()`, and
`session.git()` are privileged host operations. Gate them in your application
before exposing this flow to untrusted users.
