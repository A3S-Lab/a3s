---
title: 会话
description: 创建会话、发送提示、流式响应和管理对话历史
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Callout } from 'fumadocs-ui/components/callout';
import { Accordion, Accordions } from 'fumadocs-ui/components/accordion';

# 会话

每个会话绑定到一个工作区目录。Agent 通过 `agent.session(workspace, options?)` 创建会话。会话持有自己的 LLM 客户端、对话历史和工具上下文。

生成 API — `send()` 和 `stream()` — 将提示发送给 LLM 并返回响应。Agent 循环自动处理工具执行。

## 创建会话

<Tabs groupId="lang" items={['TypeScript', 'Python']}>
<Tab value="TypeScript">
```typescript
import { Agent } from '@a3s-lab/code';

const agent = await Agent.create('agent.hcl');

// Default session
const session = agent.session('/my-project');

// Session with model override
const session = agent.session('/my-project', {
  model: 'openai/gpt-4o',
});
```
</Tab>
<Tab value="Python">
```python
from a3s_code import Agent

agent = Agent.create("agent.hcl")

# Default session
session = agent.session("/my-project")

# Session with model override
session = agent.session("/my-project", model="openai/gpt-4o")
```
</Tab>
</Tabs>

## SessionOptions

<TypeTable
  type={{
    "模型覆盖": {
      description: "TypeScript: model: 'provider/model' · Python: model='provider/model' · 描述: 格式：provider/model（例如 openai/gpt-4o）",
    },
    "Skill 目录": {
      description: "TypeScript: skillDirs: ['path'] · Python: skill_dirs=['path'] · 描述: 额外的 skill 文件扫描目录（与全局 skill_dirs 合并）",
    },
    "Agent 目录": {
      description: "TypeScript: agentDirs: ['path'] · Python: agent_dirs=['path'] · 描述: 额外的 agent 文件扫描目录（与全局 agent_dirs 合并）",
    },
    "解析重试": {
      description: "TypeScript: maxParseRetries: n · Python: max_parse_retries=n · 描述: 连续工具参数格式错误的最大重试次数（默认：2）",
    },
    "工具超时": {
      description: "TypeScript: toolTimeoutMs: ms · Python: tool_timeout_ms=ms · 描述: 每个工具的执行超时时间（毫秒）；超时后错误会反馈给 LLM（默认：无）",
    },
    "熔断器": {
      description: "TypeScript: circuitBreakerThreshold: n · Python: circuit_breaker_threshold=n · 描述: 非流式模式下 LLM API 失败的最大次数（默认：3）",
    },
    "弹性套件": {
      description: "描述: 启用 parse_retries=2、tool_timeout=120s、circuit_breaker=3",
    },
    "沙箱": {
      description: "描述: 通过 A3S Box MicroVM 路由 bash 工具（通过 `with_sandbox_handle()` 提供实现）",
    },
    "提示词插槽": {
      description: "TypeScript: role/guidelines/responseStyle/extra · Python: opts.role/guidelines/response_style/extra · 描述: 插槽式系统提示词自定义",
    },
  }}
/>

模型必须在 agent 配置文件的 `providers` 中定义。格式为 `provider_name/model_id`。

全局 `skill_dirs` 和 `agent_dirs` 在 agent 配置中设置。每个会话的目录会与全局目录合并 — 详见 [Skills](/cn/docs/code/skills)。

## 系统提示词自定义（插槽式）

在不覆盖核心 Agent 能力的前提下自定义 Agent 行为。四个命名插槽允许注入自定义个性，同时保留工具使用策略、自主行为和完成标准：

| 插槽 | 位置 | 行为 |
|------|------|------|
| `role` | 核心提示词之前 | 替换默认的 "You are A3S Code..." 身份行 |
| `guidelines` | 核心提示词之后 | 作为 `## Guidelines` 章节追加 |
| `response_style` | 替换章节 | 替换默认的 `## Response Format` 章节 |
| `extra` | 提示词末尾 | 自由格式指令（向后兼容旧版 system_prompt） |

<Tabs groupId="lang" items={['TypeScript', 'Python']}>
<Tab value="TypeScript">
```typescript
const session = agent.session('/my-project', {
  role: 'You are a senior Rust developer.',
  guidelines: 'Use clippy. No unwrap(). Prefer Result.',
  responseStyle: 'Be concise. Use bullet points.',
  extra: 'This project uses tokio and axum.',
});
```
</Tab>
<Tab value="Python">
```python
from a3s_code import SessionOptions

opts = SessionOptions()
opts.role = "You are a senior Rust developer."
opts.guidelines = "Use clippy. No unwrap(). Prefer Result."
opts.response_style = "Be concise. Use bullet points."
opts.extra = "This project uses tokio and axum."
session = agent.session("/my-project", options=opts)
```
</Tab>
</Tabs>

详见[提示词插槽示例](/cn/docs/code/examples/prompt-slots)。

## 错误恢复与弹性

三层错误恢复机制保护长时间运行的会话：

<Accordions>
<Accordion title="解析错误恢复">
当 LLM 返回格式错误的工具参数（`__parse_error`）时，错误会作为工具结果反馈给模型以便自我修正。连续失败 `max_parse_retries` 次后循环中止。
</Accordion>
<Accordion title="工具执行超时">
每个工具调用都包装在 `tokio::time::timeout` 中。超时的工具会产生错误消息反馈给 LLM；会话继续运行。
</Accordion>
<Accordion title="熔断器">
瞬态 LLM API 故障（网络错误、速率限制）在非流式模式下会以短指数退避重试最多 `circuit_breaker_threshold` 次。在流式模式下，任何故障都是致命的（事件无法重放）。
</Accordion>
</Accordions>

<Tabs groupId="lang" items={['TypeScript', 'Python']}>
<Tab value="TypeScript">
```typescript
// Individual controls
const session = agent.session('.', {
  maxParseRetries: 3,        // bail after 3 consecutive parse errors
  toolTimeoutMs: 30000,      // 30s per tool
  circuitBreakerThreshold: 5 // retry LLM up to 5 times
});
```
</Tab>
<Tab value="Python">
```python
# Individual controls
session = agent.session(".",
  max_parse_retries=3,        # bail after 3 consecutive parse errors
  tool_timeout_ms=30000,      # 30s per tool
  circuit_breaker_threshold=5 # retry LLM up to 5 times
)
```
</Tab>
</Tabs>

## 发送（非流式）

<Tabs groupId="lang" items={['TypeScript', 'Python']}>
<Tab value="TypeScript">
```typescript
const result = await session.send('What files handle authentication?');
console.log(result.text);
console.log(`Tools: ${result.toolCallsCount}, Tokens: ${result.totalTokens}`);
```
</Tab>
<Tab value="Python">
```python
result = session.send("What files handle authentication?")
print(result.text)
print(f"Tools: {result.tool_calls_count}, Tokens: {result.total_tokens}")
```
</Tab>
</Tabs>

## 带附件发送（视觉）

发送图片附件和文本提示。需要支持视觉的模型（Claude Sonnet、GPT-4o）。

<Tabs groupId="lang" items={['TypeScript', 'Python']}>
<Tab value="TypeScript">
```typescript
const image = await fs.readFile('screenshot.png');
const result = await session.sendWithAttachments(
  "What's in this screenshot?",
  [{ data: image, mediaType: 'image/png' }],
);
console.log(result.text);
```
</Tab>
<Tab value="Python">
```python
from a3s_code import Attachment

image = Attachment.from_file("screenshot.png")
result = session.send_with_attachments(
    "What's in this screenshot?",
    [image],
)
print(result.text)
```
</Tab>
</Tabs>

支持的图片格式：JPEG、PNG、GIF、WebP。

流式变体：

### 工具图片输出

工具可以在文本输出旁返回图片。当工具返回图片时，它们会作为多模态内容块包含在发送给 LLM 的工具结果消息中。

## 流式

<Tabs groupId="lang" items={['TypeScript', 'Python']}>
<Tab value="TypeScript">
```typescript
// EventStream has .next() method — use while loop
const stream = await session.stream('Refactor the auth module');
while (true) {
  const { value, done } = await stream.next();
  if (done || !value) break;
  if (value.type === 'text_delta') process.stdout.write(value.text);
  if (value.type === 'tool_start') console.log(`\n🔧 ${value.toolName}`);
  if (value.type === 'tool_end') console.log(`  → ${value.toolOutput?.slice(0, 100)}`);
}

// For multiple prompts, create separate stream instances
const stream2 = await session.stream('Explain src/main.rs');
while (true) {
  const { value, done } = await stream2.next();
  if (done || !value) break;
  if (value.type === 'text_delta') process.stdout.write(value.text);
}
```
</Tab>
<Tab value="Python">
```python
# Sync iteration (works without an event loop)
for event in session.stream("Refactor the auth module"):
    if event.event_type == "text_delta":
        print(event.text, end="", flush=True)
    elif event.event_type == "tool_start":
        print(f"\n🔧 {event.tool_name}")
    elif event.event_type == "tool_end":
        print(f"  → {event.tool_output[:100]}")

# Async iteration (inside async def)
async for event in session.stream("Refactor the auth module"):
    if event.event_type == "text_delta":
        print(event.text, end="", flush=True)
    elif event.event_type == "end":
        print(f"\nDone — {event.total_tokens} tokens")
        break
```
</Tab>
</Tabs>

## BTW — 临时旁路问题

`btw()` 方法允许你提出旁路问题，而不会影响对话历史。完整参考请见[指令与调度](/cn/docs/code/commands#btw--临时旁路问题)。

<Tabs groupId="lang" items={['TypeScript', 'Python']}>
<Tab value="TypeScript">
```typescript
const btw = await session.btw('法国的首都是什么？');
console.log(btw.answer);       // "巴黎"
console.log(btw.totalTokens);  // token 使用量 — 问题不添加到历史
```
</Tab>
<Tab value="Python">
```python
btw = session.btw("法国的首都是什么？")
print(btw.answer)        # "巴黎"
print(btw.total_tokens)  # token 使用量 — 问题不添加到历史
```
</Tab>
</Tabs>

## 取消正在进行的操作

调用 `session.cancel()` 可以中断正在进行的 `send()` 或 `stream()` 调用。LLM 流式输出和任何正在运行的工具执行将尽快停止。

<Tabs groupId="lang" items={['TypeScript', 'Python']}>
<Tab value="TypeScript">
```typescript
// 在 send() 运行时从另一个异步上下文取消
const sendPromise = session.send('编写一个 10,000 行的程序');

// 5 秒后取消
setTimeout(() => {
  const cancelled = session.cancel();
  console.log('已取消:', cancelled); // true
}, 5000);

await sendPromise; // 返回部分结果
```
</Tab>
<Tab value="Python">
```python
import threading

# 在 send() 运行时从另一个线程取消
def cancel_after_delay():
    import time
    time.sleep(5)
    cancelled = session.cancel()
    print(f"已取消: {cancelled}")  # True

t = threading.Thread(target=cancel_after_delay)
t.start()

result = session.send("编写一个 10,000 行的程序")  # 返回部分结果
t.join()
```
</Tab>
</Tabs>

`cancel()` 返回 `true` 表示有操作正在进行并已被取消，返回 `false` 表示没有操作正在运行。

<Callout type="info">
取消是协作式的 — 操作会在下一个 LLM 流式输出块边界或工具执行检查点停止。`send()` / `stream()` 调用会正常返回已累积的部分结果。
</Callout>

## 对话历史（Rust）

通过传递历史记录维护多轮对话：

## 直接工具执行

直接调用工具而不经过 LLM：

<Tabs groupId="lang" items={['TypeScript', 'Python']}>
<Tab value="TypeScript">
```typescript
await session.readFile('src/main.rs');
await session.bash('cargo test');
await session.glob('**/*.rs');
await session.grep('fn main');
await session.tool('write', { file_path: 'x.rs', content: '...' });
```
</Tab>
<Tab value="Python">
```python
session.read_file("src/main.rs")
session.bash("cargo test")
session.glob("**/*.rs")
session.grep("fn main")
session.tool("write", {"file_path": "x.rs", "content": "..."})
```
</Tab>
</Tabs>

## 斜杠指令与定时任务

会话在发送给 LLM 之前拦截斜杠指令。内置指令包括 `/help`、`/model`、`/cost`、`/clear`、`/compact`、`/tools`、`/mcp`、`/loop`、`/cron-list` 和 `/cron-cancel`。也可注册自定义指令。

完整参考请见[指令与调度](/cn/docs/code/commands)，包括 BTW、`/loop` 语法、调度器 API 和自定义指令注册。

## 配置

参见 [提供商与配置](/cn/docs/code/providers) 获取完整的 HCL 配置参考，包括所有字段、`env()` 函数、队列和搜索配置。

## 返回类型

### AgentResult

<TypeTable
  type={{
    "text": {
      description: "类型: string · 描述: LLM 最终响应文本",
    },
    "messages": {
      description: "类型: Vec<Message> · 描述: 完整对话历史（仅 Rust）",
    },
    "usage": {
      description: "类型: TokenUsage · 描述: Token 使用统计",
    },
    "tool_calls_count": {
      description: "类型: usize · 描述: 工具调用次数",
    },
  }}
/>

### TokenUsage

<TypeTable
  type={{
    "prompt_tokens": {
      description: "类型: usize · 描述: 输入 token 数",
    },
    "completion_tokens": {
      description: "类型: usize · 描述: 输出 token 数",
    },
    "total_tokens": {
      description: "类型: usize · 描述: 总 token 数",
    },
    "cache_read_tokens": {
      description: "类型: Option<usize> · 描述: 缓存读取的输入 token 数",
    },
    "cache_write_tokens": {
      description: "类型: Option<usize> · 描述: 缓存写入的输入 token 数",
    },
  }}
/>

### AgentEvent

`AgentEvent` 是 `#[non_exhaustive]` 的 — 在 Rust 中匹配时始终包含通配符分支。

**Agent 生命周期：**

<TypeTable
  type={{
    "Start": {
      description: "字段: prompt · 描述: 开始处理",
    },
    "TurnStart": {
      description: "字段: turn · 描述: 新轮次开始",
    },
    "TextDelta": {
      description: "字段: text · 描述: 助手的文本片段",
    },
    "TurnEnd": {
      description: "字段: turn, total_tokens · 描述: 轮次完成",
    },
    "End": {
      description: "字段: text, total_tokens · 描述: 生成完成",
    },
    "Error": {
      description: "字段: error · 描述: 发生错误",
    },
    "BtwAnswer": {
      description: "字段: question, answer, total_tokens · 描述: BTW 旁路问题已回答（不添加到历史记录）",
    },
  }}
/>

**工具执行：**

<TypeTable
  type={{
    "ToolStart": {
      description: "字段: tool_id, tool_name · 描述: 工具调用开始",
    },
    "ToolEnd": {
      description: "字段: tool_id, tool_name, tool_output, exit_code · 描述: 工具调用完成",
    },
    "ToolOutputDelta": {
      description: "字段: tool_id, tool_name, text · 描述: 工具输出增量",
    },
  }}
/>

**HITL 和权限：**

<TypeTable
  type={{
    "ConfirmationRequired": {
      description: "字段: tool_id, tool_name, args, timeout_ms · 描述: 需要 HITL 确认",
    },
    "ConfirmationReceived": {
      description: "字段: tool_id, approved, reason · 描述: 收到 HITL 确认",
    },
    "ConfirmationTimeout": {
      description: "字段: tool_id, action_taken · 描述: HITL 确认超时",
    },
    "PermissionDenied": {
      description: "字段: tool_id, tool_name, args, reason · 描述: 工具被权限策略阻止",
    },
  }}
/>

**上下文和记忆：**

<TypeTable
  type={{
    "ContextResolving": {
      description: "字段: providers · 描述: 上下文解析开始",
    },
    "ContextResolved": {
      description: "字段: total_items, total_tokens · 描述: 上下文解析完成",
    },
    "MemoryStored": {
      description: "描述: 记忆项已存储",
    },
    "MemoryRecalled": {
      description: "描述: 记忆项已召回",
    },
    "MemoriesSearched": {
      description: "描述: 记忆搜索完成",
    },
    "MemoryCleared": {
      description: "描述: 记忆已清除",
    },
  }}
/>

**任务、子代理和 Lane 队列：**

<TypeTable
  type={{
    "TaskUpdated": {
      description: "字段: session_id, tasks · 描述: 任务列表变更",
    },
    "SubagentStart": {
      description: "描述: 子代理执行开始",
    },
    "ExternalTaskPending": {
      description: "描述: 外部任务已入队",
    },
    "ExternalTaskCompleted": {
      description: "描述: 外部任务已完成",
    },
    "CommandDeadLettered": {
      description: "描述: Lane 命令进入死信队列",
    },
    "CommandRetry": {
      description: "描述: Lane 命令重试",
    },
    "QueueAlert": {
      description: "描述: 队列告警触发",
    },
  }}
/>

## API 参考

### Agent

<TypeTable
  type={{
    "new": {
      description: "签名: Agent::new(config: &str) -> Result<Agent> · 描述: 从 HCL 文件或字符串加载",
    },
    "from_config": {
      description: "签名: Agent::from_config(config: AgentConfig) -> Result<Agent> · 描述: 从结构体加载",
    },
    "session": {
      description: "签名: agent.session(workspace, options?) -> Result<AgentSession> · 描述: 创建绑定工作区的会话",
    },
  }}
/>

### AgentSession

<TypeTable
  type={{
    "发送提示": {
      description: "Python: session.send(prompt) · TypeScript: await session.send(prompt)",
    },
    "流式事件": {
      description: "Python: session.stream(prompt) · TypeScript: await session.stream(prompt)",
    },
    "BTW（旁路问题）": {
      description: "Python: session.btw(question) · TypeScript: await session.btw(question) · 返回 BtwResult，包含答案和 token 使用量。问题/答案不会添加到历史记录。",
    },
    "带图片发送": {
      description: "Python: session.send_with_attachments(p, [img]) · TypeScript: await session.sendWithAttachments(p, [img])",
    },
    "读取文件": {
      description: "Python: session.read_file(path) · TypeScript: await session.readFile(path)",
    },
    "运行 bash": {
      description: "Python: session.bash(cmd) · TypeScript: await session.bash(cmd)",
    },
    "Glob": {
      description: "Python: session.glob(pattern) · TypeScript: await session.glob(pattern)",
    },
    "Grep": {
      description: "Python: session.grep(pattern) · TypeScript: await session.grep(pattern)",
    },
    "调用工具": {
      description: "Python: session.tool(name, args) · TypeScript: await session.tool(name, args)",
    },
    "历史记录": {
      description: "Python: session.history() · TypeScript: session.history()",
    },
  }}
/>

### SessionOptions

<TypeTable
  type={{
    "模型": {
      description: "Python: model='provider/model' · TypeScript: model: 'provider/model' · 默认值: 配置默认值",
    },
    "宽松模式": {
      description: "允许所有工具无需确认即可执行。基本使用必需，除非配置了 ConfirmationProvider。Python: permissive=True · TypeScript: permissive: true · 默认值: false。生产环境配置请参考安全文档。",
    },
    "规划": {
      description: "Python: planning=True · TypeScript: planning: true · 默认值: false",
    },
    "目标跟踪": {
      description: "Python: goal_tracking=True · TypeScript: goalTracking: true · 默认值: false",
    },
    "内置 skills": {
      description: "Python: builtin_skills=True · TypeScript: builtinSkills: true · 默认值: false",
    },
    "Skill 目录": {
      description: "Python: skill_dirs=[path] · TypeScript: skillDirs: [path] · 默认值: []",
    },
    "默认安全": {
      description: "Python: security_provider=DefaultSecurityProvider() · TypeScript: securityProvider: new DefaultSecurityProvider() · 默认值: false",
    },
    "文件记忆": {
      description: "Python: memory_store=FileMemoryStore(path) · TypeScript: memoryStore: new FileMemoryStore(path) · 默认值: None",
    },
    "自动压缩": {
      description: "Python: auto_compact=True · TypeScript: autoCompact: true · 默认值: false",
    },
    "自动压缩阈值": {
      description: "Python: auto_compact_threshold=f · TypeScript: autoCompactThreshold: f · 默认值: 0.8",
    },
    "文件系统上下文": {
      description: "Python: fs_context=path · TypeScript: fsContext: path · 默认值: None",
    },
    "队列配置": {
      description: "Python: queue_config=cfg · TypeScript: queueConfig: cfg · 默认值: None",
    },
    "解析重试": {
      description: "Python: max_parse_retries=n · TypeScript: maxParseRetries: n · 默认值: 2",
    },
    "工具超时": {
      description: "Python: tool_timeout_ms=ms · TypeScript: toolTimeoutMs: ms · 默认值: None",
    },
    "熔断器": {
      description: "Python: circuit_breaker_threshold=n · TypeScript: circuitBreakerThreshold: n · 默认值: 3",
    },
    "弹性默认值": {
      description: "",
    },
    "沙箱句柄": {
      description: "类型: Arc<dyn BashSandbox> · 默认值: None · 通过 `with_sandbox_handle()` 提供具体实现，将 `bash` 路由到 MicroVM 沙箱。参见[隔离](/cn/docs/code/isolation)。",
    },
    "权限检查器": {
      description: "默认值: PermissionPolicy",
    },
    "确认管理器": {
      description: "默认值: None",
    },
    "安全提供者": {
      description: "默认值: None",
    },
    "上下文提供者": {
      description: "默认值: None",
    },
    "记忆存储": {
      description: "默认值: InMemoryStore",
    },
    "Hook 引擎": {
      description: "默认值: HookEngine",
    },
  }}
/>

### AgentResponse

<TypeTable
  type={{
    "文本": {
      description: "Python: result.text · TypeScript: result.text",
    },
    "总 token 数": {
      description: "Python: result.usage.total_tokens · TypeScript: result.usage.totalTokens",
    },
    "工具调用次数": {
      description: "Python: result.tool_calls_count · TypeScript: result.toolCallsCount",
    },
  }}
/>
