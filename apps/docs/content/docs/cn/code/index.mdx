---
title: A3S Code
description: 基于 Rust 的可嵌入 AI 编程智能体框架，原生支持 Node.js 和 Python 绑定
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Callout } from 'fumadocs-ui/components/callout';
import { Steps, Step } from 'fumadocs-ui/components/steps';

# A3S Code

**基于 Rust 的可嵌入 AI 编程智能体框架** — 构建能够读写、执行代码的 AI 智能体，支持工具调用、任务规划、安全控制和多机器分布式部署。

```rust
let agent = Agent::new("agent.hcl").await?;
let session = agent.session(".", None)?;
let result = session.send("重构 auth 模块使用 JWT", None).await?;
```

<Callout type="info">
**测试覆盖：** 1373 个测试，100% 通过率 | **扩展点：** 19 个基于 trait 的扩展点 | **内置工具：** 12 个
</Callout>

## 快速开始

<Steps>
<Step>

### 安装

<Tabs groupId="lang" items={['Rust', 'TypeScript', 'Python']}>
<Tab value="Rust">
```bash
cargo add a3s-code-core
```
</Tab>
<Tab value="TypeScript">
```bash
npm install @a3s-lab/code
```
</Tab>
<Tab value="Python">
```bash
pip install a3s-code
```
</Tab>
</Tabs>

</Step>
<Step>

### 配置

创建 `agent.hcl`：

```hcl
default_model = "anthropic/claude-sonnet-4-20250514"

providers {
  name    = "anthropic"
  api_key = env("ANTHROPIC_API_KEY")
}
```

</Step>
<Step>

### 基本用法

<Tabs groupId="lang" items={['Rust', 'TypeScript', 'Python']}>
<Tab value="Rust">
```rust
use a3s_code_core::{Agent, SessionOptions};

let agent = Agent::new("agent.hcl").await?;
let session = agent.session(".", Some(
    SessionOptions::new()
        .with_default_security()
        .with_builtin_skills()
        .with_planning(true)
))?;

let result = session.send("重构 auth 模块并更新测试", None).await?;
println!("{}", result.text);
```
</Tab>
<Tab value="TypeScript">
```typescript
import { Agent } from '@a3s-lab/code';

const agent = await Agent.create('agent.hcl');
const session = agent.session('.', {
  defaultSecurity: true,
  builtinSkills: true,
  planning: true,
});

const result = await session.send('重构 auth 模块并更新测试');
console.log(result.text);
```
</Tab>
<Tab value="Python">
```python
from a3s_code import Agent

agent = Agent.create("agent.hcl")
session = agent.session(".", default_security=True, builtin_skills=True, planning=True)

result = session.send("重构 auth 模块并更新测试")
print(result.text)
```
</Tab>
</Tabs>

</Step>
</Steps>

## 文档

| 分类 | 页面 |
|------|------|
| **核心** | [会话](/cn/docs/code/sessions) · [工具](/cn/docs/code/tools) · [任务](/cn/docs/code/tasks) · [技能](/cn/docs/code/skills) |
| **安全** | [安全](/cn/docs/code/security) |
| **基础设施** | [架构](/cn/docs/code/architecture) · [通道队列](/cn/docs/code/lane-queue) · [多机器](/cn/docs/code/multi-machine) |
| **扩展** | [提供商](/cn/docs/code/providers) · [MCP](/cn/docs/code/mcp) · [子智能体](/cn/docs/code/subagents) · [上下文](/cn/docs/code/context) · [记忆](/cn/docs/code/memory) · [钩子](/cn/docs/code/hooks) · [持久化](/cn/docs/code/persistence) · [遥测](/cn/docs/code/telemetry) |
