---
title: A3S Code
description: 用于构建Agentic AI 应用的 Rust 框架，原生支持 Node.js 和 Python 绑定
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Callout } from 'fumadocs-ui/components/callout';
import { Steps, Step } from 'fumadocs-ui/components/steps';

# A3S Code

**用于构建 Agentic AI 应用的 Rust 框架** — 将能够读写、执行代码的智能体嵌入任意应用。工具调用、任务规划、安全控制、多机器分布式部署 — 原生支持 Node.js 和 Python 绑定。

<Callout type="info">
**测试覆盖：** 1501 个测试，100% 通过率 | **扩展点：** 20 个基于 trait 的扩展点 | **内置工具：** 18 个 | **内置 Skills：** 7 个
</Callout>

## 为什么选择 A3S Code？

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 mt-6">
  <div className="border rounded-lg p-4">
    <h3 className="text-lg font-semibold mb-2">🔧 可嵌入</h3>
    <span className="text-sm text-muted-foreground">Rust 库，非服务。通过 Node.js/Python 绑定集成到你的应用中。</span>
  </div>

  <div className="border rounded-lg p-4">
    <h3 className="text-lg font-semibold mb-2">🛡️ 默认安全</h3>
    <span className="text-sm text-muted-foreground">权限系统、HITL 确认、技能工具限制、错误恢复（解析重试、工具超时、熔断器）。</span>
  </div>

  <div className="border rounded-lg p-4">
    <h3 className="text-lg font-semibold mb-2">🔌 可扩展</h3>
    <span className="text-sm text-muted-foreground">20 个基于 trait 的扩展点，可替换任意策略实现。</span>
  </div>

  <div className="border rounded-lg p-4">
    <h3 className="text-lg font-semibold mb-2">📈 可伸缩</h3>
    <span className="text-sm text-muted-foreground">基于 Lane 的优先级队列，支持多机器任务分发。</span>
  </div>
</div>

## 快速开始

<Steps>
<Step>

### 安装

<Tabs groupId="lang" items={['TypeScript', 'Python']}>
<Tab value="TypeScript">
```bash
npm install @a3s-lab/code
```
</Tab>
<Tab value="Python">
```bash
pip install a3s-code
```
</Tab>
</Tabs>

</Step>
<Step>

### 配置

创建 `~/.a3s/config.hcl`（或在项目根目录创建 `./agent.hcl`）：

<Tabs groupId="provider" items={['Anthropic', 'OpenAI 兼容']}>
<Tab value="Anthropic">
```hcl
default_model = "anthropic/claude-sonnet-4-20250514"

providers {
  name    = "anthropic"
  api_key = env("ANTHROPIC_API_KEY")

  models {
    id        = "claude-sonnet-4-20250514"
    name      = "Claude Sonnet 4"
    tool_call = true
  }
}
```
</Tab>
<Tab value="OpenAI 兼容">
```hcl
# 适用于 DeepSeek、Kimi、Together AI、Groq、Ollama、vLLM 等
default_model = "openai/gpt-4o"

providers {
  name     = "openai"
  api_key  = env("OPENAI_API_KEY")
  base_url = "https://api.openai.com/v1"   # 改为你的服务商 URL

  models {
    id        = "gpt-4o"
    name      = "GPT-4o"
    tool_call = true
  }
}
```
</Tab>
</Tabs>

然后导出 API Key（macOS/Linux：`export ANTHROPIC_API_KEY="sk-ant-..."`，PowerShell：`$env:ANTHROPIC_API_KEY`）。

<Callout type="info">
任何实现了 OpenAI Chat Completions API 的服务都可以作为 provider — 只需设置 `base_url`。详见 [提供商配置](/cn/docs/code/providers) 了解 Ollama、GPUStack、A3S Power、OneAPI 等完整配置参考。
</Callout>

</Step>
<Step>

### 基本用法

<Tabs groupId="lang" items={['TypeScript', 'Python']}>
<Tab value="TypeScript">
```typescript
import { Agent } from '@a3s-lab/code';

const agent = await Agent.create('agent.hcl');
const session = agent.session('.', {
  builtinSkills: true,
  planning: true,
});

const result = await session.send('重构 auth 模块并更新测试');
console.log(result.text);
```
</Tab>
<Tab value="Python">
```python
from a3s_code import Agent

agent = Agent.create("agent.hcl")
session = agent.session(".", builtin_skills=True, planning=True)

result = session.send("重构 auth 模块并更新测试")
print(result.text)
```
</Tab>
</Tabs>

</Step>
</Steps>

## 文档

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 mt-6">
  <div className="border rounded-lg p-4">
    <h3 className="text-lg font-semibold mb-2">核心</h3>
    <ul className="text-sm text-muted-foreground space-y-1">
      <li><a href="/cn/docs/code/sessions">会话</a> — 生命周期、send/stream、斜杠指令、定时任务、视觉</li>
      <li><a href="/cn/docs/code/tools">工具</a> — 18 个内置工具参考，包含 agentic_search 与 agentic_parse</li>
      <li><a href="/cn/docs/code/tasks">任务</a> — 规划与 wave 并行执行</li>
      <li><a href="/cn/docs/code/skills">技能</a> — 技能系统与工具限制</li>
    </ul>
  </div>

  <div className="border rounded-lg p-4">
    <h3 className="text-lg font-semibold mb-2">驾驭工程</h3>
    <ul className="text-sm text-muted-foreground space-y-1">
      <li><a href="/cn/docs/code/security">安全</a> — 权限规则（Deny/Allow/Ask）、HITL、内置 PII 脱敏与注入检测</li>
      <li><a href="/cn/docs/code/hooks">钩子</a> — 11 个生命周期拦截点，自动传播到全部子智能体</li>
      <li><a href="/cn/docs/code/orchestrator">编排器</a> — 子智能体暂停、恢复、取消与提示注入</li>
      <li><a href="/cn/docs/code/limits">运行时限制</a> — 工具轮次上限、超时、熔断器、自动压缩</li>
      <li><a href="/cn/docs/code/isolation">隔离策略</a> — 工作区边界、文件版本历史、Git 工作树、MicroVM 沙箱</li>
    </ul>
  </div>

  <div className="border rounded-lg p-4">
    <h3 className="text-lg font-semibold mb-2">基础设施</h3>
    <ul className="text-sm text-muted-foreground space-y-1">
      <li><a href="/cn/docs/code/architecture">架构</a> — 系统设计概览</li>
      <li><a href="/cn/docs/code/lane-queue">通道队列</a> — 优先级路由与队列特性</li>
      <li><a href="/cn/docs/code/multi-machine">多机器</a> — 水平扩展指南</li>
    </ul>
  </div>

  <div className="border rounded-lg p-4">
    <h3 className="text-lg font-semibold mb-2">扩展</h3>
    <ul className="text-sm text-muted-foreground space-y-1">
      <li><a href="/cn/docs/code/providers">提供商</a> — LLM 提供商与完整配置参考</li>
      <li><a href="/cn/docs/code/plugins">插件</a> — 内置核心能力之外的按需扩展工具 + 技能包</li>
      <li><a href="/cn/docs/code/mcp">MCP</a> — 外部工具集成</li>
      <li><a href="/cn/docs/code/subagents">子智能体</a> — 委托给专门智能体</li>
      <li><a href="/cn/docs/code/context">上下文</a> — RAG 提供商</li>
      <li><a href="/cn/docs/code/memory">记忆</a> — 长期记忆</li>
      <li><a href="/cn/docs/code/hooks">钩子</a> — 生命周期事件</li>
      <li><a href="/cn/docs/code/persistence">持久化</a> — 会话存储</li>
      <li><a href="/cn/docs/code/telemetry">遥测</a> — 成本追踪与指标</li>
    </ul>
  </div>
</div>
