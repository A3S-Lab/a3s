---
title: "Provider 配置"
description: "v2.1.0 的 ACL provider 配置与环境变量注入"
---

# Provider 配置

A3S Code v2.1.0 从 ACL 读取运行时配置。配置源可以是 `.acl` 文件路径，也可以是内联 ACL 字符串。JSON 和旧 HCL 配置不再支持。

## 基本结构

```text
default_model = "provider/model-id"

providers "provider" {
  apiKey = env("PROVIDER_API_KEY")
  baseUrl = env("PROVIDER_BASE_URL")

  models "model-id" {
    name = "Human readable model name"
    tool_call = true
  }
}

storage_backend = "file"
```

`apiKey` 与 `api_key` 都可用，`baseUrl` 与 `base_url` 都可用。密钥建议只通过环境变量注入，仓库里只提交模板或非敏感默认值。

## Anthropic

```text
default_model = "anthropic/claude-sonnet-4-20250514"

providers "anthropic" {
  apiKey = env("ANTHROPIC_API_KEY")
}
```

## OpenAI 兼容服务

任何兼容 OpenAI Chat Completions 的服务都可以通过 `openai` provider 接入：

```text
default_model = "openai/gpt-4o"

providers "openai" {
  apiKey = env("A3S_OPENAI_API_KEY")
  baseUrl = env("A3S_OPENAI_BASE_URL")
}
```

MiniMax 的发布 smoke 测试也使用同一个 provider：

```text
default_model = "openai/MiniMax-M2.7-highspeed"

providers "openai" {
  apiKey = env("A3S_OPENAI_API_KEY")
  baseUrl = env("A3S_OPENAI_BASE_URL")

  models "MiniMax-M2.7-highspeed" {
    name = "MiniMax M2.7 Highspeed"
    tool_call = true
  }
}
```

当 `A3S_OPENAI_*` 未设置时，测试脚本会把 `MINIMAX_API_KEY` 与 `MINIMAX_BASE_URL` 映射过去。

## 存储

```text
storage_backend = "memory"
storage_backend = "file"
storage_url = ".a3s/sessions"
```

短测试使用 memory，需恢复的本地 session 使用 file。

## 真实配置集成测试

发布 preflight 不会修改真实 `.a3s/config.acl`。脚本会提取 provider 值，把临时 ACL 改写成 `env(...)`，再把这些值注入当前进程环境，并验证运行时可以正确解析。

```bash
A3S_CONFIG_FILE=/path/to/.a3s/config.acl \
  A3S_OPENAI_API_KEY="$A3S_OPENAI_API_KEY" \
  A3S_OPENAI_BASE_URL="$A3S_OPENAI_BASE_URL" \
  scripts/real_config_env_integration.sh
```

SDK 对齐由单独的真实 provider smoke 脚本覆盖：

```bash
A3S_CONFIG_FILE=/path/to/.a3s/config.acl \
  scripts/sdk_real_config_env_integration.sh
```

该脚本会让 Node 和 Python 使用同一个临时 env 注入 ACL，覆盖 `Agent.create`、`send`、`btw`、`program`、工具 schema、run replay 和委派 smoke 路径。
