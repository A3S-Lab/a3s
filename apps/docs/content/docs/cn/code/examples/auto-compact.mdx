---
title: "自动压缩"
description: "让长 session 保持在上下文预算内"
---

# 自动压缩

```ts
const session = agent.session('/repo', {
  autoCompact: true,
  autoCompactThreshold: 0.75,
  continuationEnabled: true,
  maxContinuationTurns: 3,
});
```

长 session 需要让 runtime 管理 context pressure 时，可以启用自动压缩。
