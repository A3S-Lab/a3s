---
title: Hooks
description: 生命周期事件拦截 — PreToolUse、PostToolUse、PrePrompt、PostResponse、OnError
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Callout } from 'fumadocs-ui/components/callout';

# Hooks

Hooks 允许你拦截 agent 生命周期事件，用于审计、修改或阻止操作。在会话上注册 `Hook` + `HookHandler` 对。

## Hook 事件类型

<TypeTable
  type={{
    "PreToolUse": {
      description: "触发时机: 工具执行前 · 可阻止？: ✅",
    },
    "PostToolUse": {
      description: "触发时机: 工具完成后 · 可阻止？: ❌",
    },
    "PrePrompt": {
      description: "触发时机: 提示发送到 LLM 前 · 可阻止？: ✅",
    },
    "PostResponse": {
      description: "触发时机: LLM 响应后 · 可阻止？: ❌",
    },
    "OnError": {
      description: "触发时机: 任何 agent 错误时 · 可阻止？: ❌",
    },
    "GenerateStart": {
      description: "触发时机: LLM 生成开始 · 可阻止？: ❌",
    },
    "GenerateEnd": {
      description: "触发时机: LLM 生成结束 · 可阻止？: ❌",
    },
    "SessionStart\` / \`SessionEnd": {
      description: "触发时机: 会话生命周期 · 可阻止？: ❌",
    },
  }}
/>

## 审计 Hook（记录所有工具调用）

<Tabs groupId=\"lang\" items={['TypeScript', 'Python']}>
<Tab value="TypeScript">
```typescript
const toolLog: string[] = [];

session.registerHook('audit', 'pre_tool_use', (event) => {
  const tool = event.tool || 'unknown';
  console.log(`→ tool: ${tool}`);
  toolLog.push(tool);
  return null; // 允许执行
});

console.log(`Hooks: ${session.hookCount()}`);

const result = await session.send('List files in the workspace');

console.log('Tools called:', toolLog);
session.unregisterHook('audit');
```

**运行：** `npx ts-node examples/test_advanced_features.ts`
**源码：** [`sdk/node/examples/test_advanced_features.ts`](https://github.com/A3S-Lab/Code/blob/main/sdk/node/examples/test_advanced_features.ts)
</Tab>
<Tab value="Python">
```python
tool_log = []

def on_pre_tool_use(event):
    tool = event.get("tool", "unknown")
    print(f"→ tool: {tool}")
    tool_log.append(tool)
    return None  # 允许执行

session.register_hook("audit", "pre_tool_use", on_pre_tool_use)
print(f"Hooks: {session.hook_count()}")

result = session.send("List files in the workspace")

print(f"Tools called: {tool_log}")
session.unregister_hook("audit")
```

**运行：** `python examples/test_advanced_features.py`
**源码：** [`sdk/python/examples/test_advanced_features.py`](https://github.com/A3S-Lab/Code/blob/main/sdk/python/examples/test_advanced_features.py)
</Tab>
</Tabs>

## 阻止 Hook（拒绝特定工具）

<Tabs groupId=\"lang\" items={['TypeScript', 'Python']}>
<Tab value="TypeScript">
```typescript
session.registerHook('no-bash', 'pre_tool_use', (event) => {
  if (event.tool === 'bash') {
    return { action: 'block', reason: 'bash is not allowed' };
  }
  return null;
});
```
</Tab>
<Tab value="Python">
```python
def deny_bash(event):
    if event.get("tool") == "bash":
        return {"action": "block", "reason": "bash is not allowed"}
    return None

session.register_hook("no-bash", "pre_tool_use", deny_bash)
```
</Tab>
</Tabs>

## 哨兵智能体（监控 + 阻断 + 告警）

哨兵在 session 上附加两个 hooks——一个在命令运行前阻断危险操作，一个在输出中检测敏感数据。Hooks 会自动传播到任意深度的每个子 agent。

<Tabs groupId="lang" items={['TypeScript', 'Python']}>
<Tab value="TypeScript">
```typescript
import { Agent } from '@a3s-lab/code';

const BLOCKED = ['rm -rf', 'mkfs', 'dd if=', 'DROP TABLE', 'curl | sh'];
const SENSITIVE = ['password=', 'secret=', 'api_key=', 'token='];

function attachSentinel(session: ReturnType<Agent['session']>, label = '') {
  const tag = label ? `sentinel-${label}-` : 'sentinel-';

  session.registerHook(
    `${tag}pre`, 'pre_tool_use', null, { priority: 1 },
    (event) => {
      const p = (event.payload ?? event) as Record<string, unknown>;
      if (!['bash', 'shell'].includes(String(p.tool ?? ''))) return null;
      const cmd = String((p.args as Record<string, unknown>)?.command ?? '');
      for (const pat of BLOCKED) {
        if (cmd.toLowerCase().includes(pat.toLowerCase())) {
          console.log(`⛔  已阻断: '${pat}'`);
          return { action: 'block', reason: `[哨兵] '${pat}'` };
        }
      }
      return null;
    },
  );

  session.registerHook(
    `${tag}post`, 'post_tool_use', null,
    { priority: 1, asyncExecution: true },
    (event) => {
      const p = (event.payload ?? event) as Record<string, unknown>;
      const output = String((p.result as Record<string, unknown>)?.output ?? '');
      for (const kw of SENSITIVE) {
        if (output.toLowerCase().includes(kw.toLowerCase())) {
          console.log(`⚠️   告警: 输出中检测到敏感关键词 '${kw}'`);
          break;
        }
      }
      return null;
    },
  );
}

const agent = await Agent.create('agent.hcl');
const session = agent.session('/workspace', { permissive: true });
attachSentinel(session);

// 安全命令 — 通过
const r1 = await session.send('使用 bash 列出 /tmp 中的文件');
console.log(r1.text);

// 危险命令 — 哨兵在 bash 运行前阻断
const r2 = await session.send('使用 bash 运行: rm -rf /tmp/test');
console.log(r2.text);
```

**源码：** [`sdk/node/examples/sentinel_agent.ts`](https://github.com/A3S-Lab/Code/blob/main/sdk/node/examples/sentinel_agent.ts)
</Tab>
<Tab value="Python">
```python
from typing import Optional
from a3s_code import Agent

BLOCKED = ["rm -rf", "mkfs", "dd if=", "DROP TABLE", "curl | sh"]
SENSITIVE = ["password=", "secret=", "api_key=", "token=", "Bearer "]

def on_pre_tool_use(event: dict) -> Optional[dict]:
    p = event.get("payload", event)
    tool = p.get("tool", "")
    if tool not in ("bash", "shell"):
        return None
    cmd = p.get("args", {}).get("command", "")
    for pat in BLOCKED:
        if pat.lower() in cmd.lower():
            print(f"⛔  已阻断: 命令中包含 '{pat}'")
            return {"action": "block", "reason": f"[哨兵] '{pat}'"}
    return None

def on_post_tool_use(event: dict) -> Optional[dict]:
    p = event.get("payload", event)
    result = p.get("result", {})
    output = result.get("output", "") if isinstance(result, dict) else ""
    for kw in SENSITIVE:
        if kw.lower() in output.lower():
            print(f"⚠️   告警: 输出中检测到敏感关键词 '{kw}'")
            break
    return None

def attach_sentinel(session, label: str = "") -> None:
    tag = f"sentinel-{label}-" if label else "sentinel-"
    session.register_hook(
        f"{tag}pre", "pre_tool_use",
        config={"priority": 1},
        handler=on_pre_tool_use,
    )
    session.register_hook(
        f"{tag}post", "post_tool_use",
        config={"priority": 1, "async_execution": True},
        handler=on_post_tool_use,
    )

agent = Agent.create("agent.hcl")
session = agent.session("/workspace", permissive=True)
attach_sentinel(session)

# 安全命令 — 通过
r = session.send("使用 bash 列出 /tmp 中的文件")
print(r.text)

# 危险命令 — 哨兵在 bash 运行前阻断
r = session.send("使用 bash 运行: rm -rf /tmp/test")
print(r.text)  # agent 解释被阻断的原因
```

**源码：** [`sdk/python/examples/sentinel_agent.py`](https://github.com/A3S-Lab/Code/blob/main/sdk/python/examples/sentinel_agent.py)
</Tab>
</Tabs>

如需为 AgentTeam 的每个成员添加哨兵，在绑定前为每个 session 附加即可：

```python
from a3s_code import Agent, Team, TeamRunner, TeamConfig

agent  = Agent.create("agent.hcl")
team   = Team("my-team", TeamConfig(max_tasks=20, max_rounds=6))
team.add_member("lead",     "lead")
team.add_member("worker",   "worker")
team.add_member("reviewer", "reviewer")

runner = TeamRunner(team)

for role in ("lead", "worker", "reviewer"):
    s = agent.session("/workspace", permissive=True)
    attach_sentinel(s, role)
    runner.bind_session(role, s)

result = runner.run_until_done("分析代码库中的安全问题")
```

<Callout type="info">
如需跨会话的持久化 hook 配置，请参阅 [Hooks](/cn/docs/code/hooks)。
</Callout>

## API 参考

### Hook 注册

<TypeTable
  type={{
    "注册 hook": {
      description: "Rust: session.register_hook(hook) · Python: session.register_hook(id, event, fn) · TypeScript: session.registerHook(id, event, fn)",
    },
    "注册处理器": {
      description: "Rust: session.register_hook_handler(id, Arc::new(h)) · Python: _（与注册合并）_ · TypeScript: _（与注册合并）_",
    },
    "注销": {
      description: "Rust: session.unregister_hook(id) · Python: session.unregister_hook(id) · TypeScript: session.unregisterHook(id)",
    },
    "计数": {
      description: "Rust: session.hook_count() · Python: session.hook_count() · TypeScript: session.hookCount()",
    },
  }}
/>

### HookResponse（Rust）

<TypeTable
  type={{
    "HookResponse::continue_()": {
      description: "允许操作继续",
    },
    "HookResponse::block(reason)": {
      description: "以原因字符串取消操作",
    },
    "HookResponse::modify(data)": {
      description: "在执行前替换事件数据",
    },
  }}
/>

### Hook 事件字段

<TypeTable
  type={{
    "PreToolUse": {
      description: "tool: String`, `args: Value",
    },
    "PostToolUse": {
      description: "tool: String`, `output: String`, `exit_code: i32",
    },
    "PrePrompt": {
      description: "prompt: String",
    },
    "PostResponse": {
      description: "text: String`, `usage: Usage",
    },
    "OnError": {
      description: "error: String`, `turn: u32",
    },
  }}
/>
