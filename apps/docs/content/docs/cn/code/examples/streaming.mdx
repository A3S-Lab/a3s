---
title: 流式传输
description: 使用 AgentEvent 的实时事件流
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { TypeTable } from 'fumadocs-ui/components/type-table';

# 流式传输

订阅 `AgentEvent` 以实时了解 agent 的执行状态 — 文本增量、工具调用、token 使用量等。

## 事件类型

<TypeTable
  type={{
    "Start": {
      description: "Agent 开始处理",
    },
    "TurnStart\` / \`TurnEnd": {
      description: "每个 LLM 轮次",
    },
    "TextDelta": {
      description: "流式文本片段",
    },
    "ToolStart\` / \`ToolEnd": {
      description: "工具执行",
    },
    "ToolOutputDelta": {
      description: "流式工具输出（bash）",
    },
    "ContextResolving\` / \`ContextResolved": {
      description: "RAG 检索",
    },
    "End": {
      description: "Agent 完成",
    },
  }}
/>

## 示例

<Tabs groupId="lang" items={['TypeScript', 'Python']}>
<Tab value="TypeScript">
```typescript
const session = agent.session('/my-project', { permissive: true });

const stream = await session.stream('Refactor the auth module');

for await (const event of stream) {
  switch (event.type) {
    case 'text_delta':
      process.stdout.write(event.text);
      break;
    case 'tool_start':
      process.stdout.write(`\n🔧 ${event.toolName}...`);
      break;
    case 'tool_end':
      console.log(` ✓ (exit=${event.exitCode})`);
      break;
    case 'turn_end':
      console.log(`\n└─ Turn ${event.turn} done (${event.totalTokens} tokens)`);
      break;
    case 'end':
      console.log(`\n■ Done — ${event.totalTokens} tokens total`);
      break;
  }
}
```

**源码：** [`sdk/node/examples/agentic_loop_demo.ts`](https://github.com/A3S-Lab/Code/blob/main/sdk/node/examples/agentic_loop_demo.ts)
</Tab>
<Tab value="Python">
```python
session = agent.session("/my-project", permissive=True)

for event in session.stream("Refactor the auth module"):
    t = event.event_type
    if t == "text_delta":
        print(event.text, end="", flush=True)
    elif t == "tool_start":
        print(f"\n🔧 {event.tool_name}...", end="")
    elif t == "tool_end":
        print(f" ✓ (exit={event.exit_code})")
    elif t == "turn_end":
        print(f"\n└─ Turn {event.turn} done ({event.total_tokens} tokens)")
    elif t == "end":
        print(f"\n■ Done — {event.total_tokens} tokens total")
        break
```

**源码：** [`sdk/python/examples/agentic_loop_demo.py`](https://github.com/A3S-Lab/Code/blob/main/sdk/python/examples/agentic_loop_demo.py)
</Tab>
</Tabs>

## 示例输出

```
┌─ Turn 1
🔧 read... ✓ (exit=0)
🔧 write... ✓ (exit=0)
└─ Turn 1 done (1842 tokens)
┌─ Turn 2
The auth module has been refactored. Key changes:
- Extracted JWT validation into a separate function
- Added proper error types
- Improved test coverage
└─ Turn 2 done (2103 tokens)
■ Done — 3945 tokens total
```

## API 参考

### stream()

<TypeTable
  type={{
    "启动流": {
      description: "Python: session.stream('prompt') · TypeScript: await session.stream('prompt')",
    },
    "接收事件": {
      description: "Python: for event in session.stream(...) · TypeScript: for await (const event of stream)",
    },
    "等待完成": {
      description: "Python: _（自动）_ · TypeScript: _（自动）_",
    },
  }}
/>

### AgentEvent 类型

<TypeTable
  type={{
    "Agent 启动": {
      description: "Rust 变体: AgentEvent::Start · Python/TypeScript type: start",
    },
    "LLM 轮次开始": {
      description: "Rust 变体: AgentEvent::TurnStart · Python/TypeScript type: turn_start · 字段: turn",
    },
    "LLM 轮次结束": {
      description: "Rust 变体: AgentEvent::TurnEnd · Python/TypeScript type: turn_end · 字段: turn, usage",
    },
    "文本片段": {
      description: "Rust 变体: AgentEvent::TextDelta · Python/TypeScript type: text_delta · 字段: text",
    },
    "工具开始": {
      description: "Rust 变体: AgentEvent::ToolStart · Python/TypeScript type: tool_start · 字段: name, args",
    },
    "工具完成": {
      description: "Rust 变体: AgentEvent::ToolEnd · Python/TypeScript type: tool_end · 字段: name, exit_code, output",
    },
    "工具输出片段": {
      description: "Rust 变体: AgentEvent::ToolOutputDelta · Python/TypeScript type: tool_output_delta · 字段: text",
    },
    "RAG 检索中": {
      description: "Rust 变体: AgentEvent::ContextResolving · Python/TypeScript type: context_resolving · 字段: query",
    },
    "RAG 检索完成": {
      description: "Rust 变体: AgentEvent::ContextResolved · Python/TypeScript type: context_resolved · 字段: chunks",
    },
    "Agent 完成": {
      description: "Rust 变体: AgentEvent::End · Python/TypeScript type: end · 字段: text, usage",
    },
  }}
/>
