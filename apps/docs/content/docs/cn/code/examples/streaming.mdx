---
title: "流式输出"
description: "在回合运行时读取 AgentEvent"
---

# 流式输出

```ts
const stream = await session.stream('运行测试并解释失败原因');

while (true) {
  const { value: event, done } = await stream.next();
  if (done) break;
  if (!event) continue;

  if (event.text) process.stdout.write(event.text);
  if (event.toolName) console.log('\n[tool]', event.toolName);
  if (event.error) console.error(event.error);
}
```

流式事件可以包含文本、工具调用、工具输出、错误、token 总数和验证摘要。
