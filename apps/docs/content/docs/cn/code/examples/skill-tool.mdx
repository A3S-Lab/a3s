---
title: "Skill 工具"
description: "在回合中搜索并应用 skills"
---

# Skill 工具

当任务需要时，可以明确要求 agent 搜索并应用 skills：

```text
搜索可用 skills，寻找发布检查指导。
应用最匹配的 skill，然后检查包元数据并构建证据。
```

驾驭层把 `search_skills` 和 `Skill` 作为核心 skill 工具暴露。Skill 管理由 SDK 注册、`skillDirs` 或项目文件完成，不再通过模型可见的管理工具处理。
