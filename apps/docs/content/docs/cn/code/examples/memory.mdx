---
title: "记忆"
description: "记住并召回任务事实"
---

# 记忆

```ts
import { FileMemoryStore } from '@a3s-lab/code';

const session = agent.session('/repo', {
  memoryStore: new FileMemoryStore('./.a3s/memory'),
});

await session.rememberSuccess('文档重写', ['grep', 'read'], '已移除过期配置说明');
const memories = await session.recallSimilar('文档发布重写', 5);
```
