---
title: 快速开始
description: 运行和管理第一个 A3S Box MicroVM
---

# 快速开始

本页只使用核心 CLI 路径。先确认 `a3s-box info` 在当前主机上报告可用的 VM 后端。

## 1. 安装

```bash
# macOS / Linux，通过 Homebrew tap
brew install a3s-lab/tap/a3s-box

# 或从源码构建
git clone https://github.com/A3S-Lab/Box.git
cd Box/src
cargo build --release
```

检查主机能力：

```bash
a3s-box version
a3s-box info
```

`info` 会报告平台、VM 后端、控制通道、bridge 后端、端口发布和 TEE 可用性。

## 2. 运行一个命令

```bash
a3s-box run --name hello alpine:latest -- echo "hello from a3s-box"
```

这个命令会在需要时拉取镜像、准备 rootfs、启动 MicroVM、运行命令、捕获输出并停止 box。

## 3. 交互式 shell

```bash
a3s-box run -it --name dev alpine:latest -- /bin/sh
```

对已运行的 box：

```bash
a3s-box exec -it dev -- /bin/sh
```

## 4. 后台服务

```bash
a3s-box run -d --name web --cpus 2 --memory 1g -p 8080:80 nginx:alpine

a3s-box ps
a3s-box port web
a3s-box logs -f web
```

端口发布只支持 `host_port:guest_port[/tcp]`。UDP、绑定指定 host IP、单端口简写和端口范围都会被拒绝。

## 5. 执行和检查

```bash
a3s-box exec web -- nginx -v
a3s-box stats --no-stream web
a3s-box inspect web
a3s-box top web
```

需要用户、目录和环境变量时：

```bash
a3s-box exec -u root -w /tmp -e DEBUG=1 web -- sh -c 'id && pwd && env | grep DEBUG'
```

## 6. 停止和删除

```bash
a3s-box stop web
a3s-box wait web
a3s-box rm web
```

只有在明确需要强制清理时才使用 `rm -f`。

## 7. 命名卷

```bash
a3s-box volume create data
a3s-box run -d --name store -v data:/data alpine:latest -- sleep 3600
a3s-box cp ./local.txt store:/data/local.txt
a3s-box exec store -- ls -la /data
a3s-box rm -f store
a3s-box volume rm data
```

## 8. Bridge 网络

```bash
a3s-box network create backend --subnet 10.89.0.0/24
a3s-box run -d --name api --network backend -p 8080:80 alpine:latest -- sleep 3600
a3s-box network inspect backend
a3s-box network rm --force backend
```

平台边界：

- Linux bridge 使用 `passt`，报告支持 outbound NAT。
- macOS bridge 使用 `netproxy` 做 peer 网络和 TCP 端口发布；不支持 bridge outbound NAT。
- macOS 工作负载需要外网时，使用默认 TSI。

## 9. 构建简单镜像

```dockerfile
# Dockerfile
FROM scratch
COPY hello.txt /hello.txt
CMD ["cat", "/hello.txt"]
```

```bash
echo hello > hello.txt
a3s-box build -t hello:local .
a3s-box run --rm hello:local
```

Dockerfile `RUN` 需要可 root 的 Linux。macOS 默认拒绝 `RUN`，避免静默在宿主机上执行构建命令。

## 10. Compose 子集

```yaml
services:
  worker:
    image: alpine:latest
    command: ["sleep", "3600"]
    environment:
      A3S_EXAMPLE: "1"
```

```bash
a3s-box compose -f compose.yaml config
a3s-box compose -f compose.yaml up -d
a3s-box compose -f compose.yaml ps
a3s-box compose -f compose.yaml down
```

## 清理

```bash
a3s-box system-prune --force
a3s-box image-prune --force
a3s-box volume prune --force
```
