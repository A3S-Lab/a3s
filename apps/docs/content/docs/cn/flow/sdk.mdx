---
title: SDK 参考
description: Rust、Python 和 TypeScript SDK 的完整 API 参考
---

import { TypeTable } from 'fumadocs-ui/components/type-table';

# SDK 参考

A3S Flow 为 Rust、Python 和 TypeScript 提供了 API 相同的 SDK。所有 SDK 都封装了相同的 Rust 运行时，提供相同的功能。

<TypeTable
  type={{
    "Rust": {
      description: "包: a3s-flow crate · 版本: 0.3.4",
    },
    "Python": {
      description: "包: pip install a3s-flow · 版本: 0.3.4",
    },
    "TypeScript": {
      description: "包: npm install @a3s-lab/flow · 版本: 0.3.4",
    },
  }}
/>

---

## Rust SDK

### 安装

```toml
[dependencies]
a3s-flow = "0.3"
tokio = { version = "1", features = ["full"] }
serde_json = "1"
```

### FlowEngine

创建和管理工作流执行的入口点。

```rust
use a3s_flow::{FlowEngine, NodeRegistry};

// 使用默认节点创建引擎
let engine = FlowEngine::new(NodeRegistry::with_defaults());

// 使用自定义注册表创建引擎
let mut registry = NodeRegistry::new();
registry.register(Arc::new(MyCustomNode));
let engine = FlowEngine::new(registry);
```

#### 方法

```rust
// 列出已注册的节点类型
fn node_types(&self) -> Vec<String>

// 开始工作流执行
async fn start(
    &self,
    definition: &Value,
    variables: HashMap<String, Value>
) -> Result<Uuid>

// 在下一个波次边界暂停执行
async fn pause(&self, id: Uuid) -> Result<()>

// 恢复暂停的执行
async fn resume(&self, id: Uuid) -> Result<()>

// 终止执行
async fn terminate(&self, id: Uuid) -> Result<()>

// 查询执行状态
async fn state(&self, id: Uuid) -> Result<ExecutionState>

// 订阅执行事件
async fn subscribe(&self, id: Uuid) -> Result<Receiver<FlowEvent>>
```

### ExecutionState

```rust
pub enum ExecutionState {
    Running,
    Paused,
    Completed(FlowResult),
    Failed(String),
    Terminated,
}

impl ExecutionState {
    pub fn as_str(&self) -> &str;
    pub fn is_terminal(&self) -> bool;
}
```

### FlowResult

```rust
pub struct FlowResult {
    pub execution_id: Uuid,
    pub outputs: HashMap<String, Value>,
    pub completed_nodes: HashSet<String>,
    pub skipped_nodes: HashSet<String>,
    pub context: HashMap<String, Value>,
}
```

### FlowEvent

```rust
pub enum FlowEvent {
    NodeStarted { node_id: String },
    NodeCompleted { node_id: String, output: Value },
    NodeSkipped { node_id: String },
    NodeFailed { node_id: String, error: String },
    WaveCompleted { wave_index: usize, node_count: usize },
    ExecutionPaused,
    ExecutionResumed,
    ExecutionCompleted { result: FlowResult },
    ExecutionFailed { error: String },
    ExecutionTerminated,
}
```

---

## Python SDK

### 安装

```bash
pip install a3s-flow
```

### FlowEngine

```python
from a3s_flow import FlowEngine

# 创建引擎
engine = FlowEngine()

# 开始工作流
execution_id = await engine.start(definition, variables)

# 暂停执行
await engine.pause(execution_id)

# 恢复执行
await engine.resume(execution_id)

# 终止执行
await engine.terminate(execution_id)

# 查询状态
state = await engine.state(execution_id)

# 订阅事件
async for event in engine.subscribe(execution_id):
    print(f"事件: {event}")
```

### State 对象

```python
{
    "status": "running" | "paused" | "completed" | "failed" | "terminated",
    "result": {  # 仅在 status == "completed" 时存在
        "execution_id": str,
        "outputs": dict,
        "completed_nodes": list[str],
        "skipped_nodes": list[str],
        "context": dict
    },
    "error": str  # 仅在 status == "failed" 时存在
}
```

### Event 对象

```python
{
    "type": "node_started" | "node_completed" | "node_skipped" | "node_failed" |
            "wave_completed" | "execution_paused" | "execution_resumed" |
            "execution_completed" | "execution_failed" | "execution_terminated",
    "node_id": str,  # 用于节点事件
    "output": any,   # 用于 node_completed
    "error": str,    # 用于 node_failed / execution_failed
    "wave_index": int,  # 用于 wave_completed
    "node_count": int,  # 用于 wave_completed
    "result": dict   # 用于 execution_completed
}
```

---

## TypeScript SDK

### 安装

```bash
npm install @a3s-lab/flow
```

### FlowEngine

```typescript
import { FlowEngine, ExecutionState, FlowEvent } from '@a3s-lab/flow';

// 创建引擎
const engine = new FlowEngine();

// 开始工作流
const executionId = await engine.start(definition, variables);

// 暂停执行
await engine.pause(executionId);

// 恢复执行
await engine.resume(executionId);

// 终止执行
await engine.terminate(executionId);

// 查询状态
const state = await engine.state(executionId);

// 订阅事件
for await (const event of engine.subscribe(executionId)) {
  console.log('事件:', event);
}
```

### ExecutionState

```typescript
type ExecutionState =
  | { status: 'running' }
  | { status: 'paused' }
  | { status: 'completed'; result: FlowResult }
  | { status: 'failed'; error: string }
  | { status: 'terminated' };

interface FlowResult {
  executionId: string;
  outputs: Record<string, any>;
  completedNodes: string[];
  skippedNodes: string[];
  context: Record<string, any>;
}
```

### FlowEvent

```typescript
type FlowEvent =
  | { type: 'node_started'; nodeId: string }
  | { type: 'node_completed'; nodeId: string; output: any }
  | { type: 'node_skipped'; nodeId: string }
  | { type: 'node_failed'; nodeId: string; error: string }
  | { type: 'wave_completed'; waveIndex: number; nodeCount: number }
  | { type: 'execution_paused' }
  | { type: 'execution_resumed' }
  | { type: 'execution_completed'; result: FlowResult }
  | { type: 'execution_failed'; error: string }
  | { type: 'execution_terminated' };
```

---

## 工作流定义

所有 SDK 都接受相同的 JSON 工作流定义格式：

```typescript
interface WorkflowDefinition {
  nodes: Node[];
  edges: Edge[];
}

interface Node {
  id: string;
  type: string;
  data?: Record<string, any>;
}

interface Edge {
  source: string;
  target: string;
}
```

### 条件执行

在 `data` 中添加 `run_if` 来条件执行节点：

```json
{
  "id": "notify",
  "type": "http-request",
  "data": {
    "url": "https://hooks.example.com/success",
    "method": "POST",
    "run_if": {
      "from": "check",
      "path": "branch",
      "op": "eq",
      "value": "success"
    }
  }
}
```

<TypeTable
  type={{
    "from": {
      description: "要读取的上游节点 ID",
    },
    "path": {
      description: "输出中的点分隔路径（例如，\"body.status\"）",
    },
    "op": {
      description: "比较运算符: eq | ne | gt | lt | gte | lte | contains",
    },
    "value": {
      description: "比较的右侧（任何 JSON 值）",
    },
  }}
/>

---

## 错误处理

### Rust

```rust
match engine.start(&definition, variables).await {
    Ok(id) => println!("已开始: {}", id),
    Err(FlowError::InvalidDefinition(msg)) => eprintln!("无效的工作流: {}", msg),
    Err(FlowError::CyclicDependency) => eprintln!("工作流包含循环"),
    Err(e) => eprintln!("错误: {}", e),
}
```

### Python

```python
try:
    execution_id = await engine.start(definition, variables)
except ValueError as e:
    print(f"无效的工作流: {e}")
except RuntimeError as e:
    print(f"执行错误: {e}")
```

### TypeScript

```typescript
try {
  const executionId = await engine.start(definition, variables);
} catch (error) {
  if (error.message.includes('cycle')) {
    console.error('工作流包含循环');
  } else {
    console.error('错误:', error.message);
  }
}
```
