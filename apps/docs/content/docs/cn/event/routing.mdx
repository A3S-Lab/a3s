---
title: 事件路由
description: Broker/Trigger 路由和事件 Sink
---

# 事件路由

`Broker` 和 `Trigger` 提供 Knative 风格的事件路由，`EventSink` 支持 `TopicSink`、`InProcessSink` 和 `LogSink` 投递目标（需要 `routing` 特性）。

详细文档请参阅[英文版](/docs/en/event/routing)。
