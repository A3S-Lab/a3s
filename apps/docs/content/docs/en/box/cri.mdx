---
title: Kubernetes CRI
description: Experimental CRI surface for evaluating A3S Box with kubelet and crictl
---

# Kubernetes CRI

A3S Box includes an experimental Kubernetes CRI server. It is useful for evaluation, development, and targeted smoke tests, but the current product priority is the local Docker-like runtime. Do not read this page as a production certification statement.

Windows CRI is intentionally unsupported.

## What Exists

| CRI area | Current state |
| --- | --- |
| RuntimeService | Pod sandbox lifecycle, container lifecycle, status, stats, selected update/status paths. |
| ImageService | Pull, list, status, filesystem info, and remove paths backed by the local image store. |
| Streaming | Exec, attach, and port-forward paths exist with Unix-socket bridge logic on Unix hosts. |
| Networking | Sandbox port mappings and an `a3s.box/network` annotation path exist for A3S bridge networks. |
| Tests | Unit tests cover fake ready-VM paths; `crictl_smoke.rs` is ignored and requires a real host setup. |

## Mental Model

```text
kubelet / crictl
  |
  | CRI v1 gRPC
  v
a3s-box-cri
  RuntimeService + ImageService + streaming server
  |
  v
A3S Box runtime and MicroVM
```

A pod sandbox maps to a Box VM. Container support is evolving around rootfs handoff and guest exec/session behavior.

## Run for Evaluation

```bash
# From the Box repository
helm install a3s-box deploy/helm/a3s-box/ \
  -n a3s-box-system --create-namespace
```

Or run the CRI binary directly in a controlled development environment and point `crictl` or kubelet at its socket.

## RuntimeClass Example

```yaml
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: a3s-box
handler: a3s-box
```

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: hello
spec:
  runtimeClassName: a3s-box
  containers:
    - name: app
      image: alpine:latest
      command: ["sleep", "3600"]
```

## Known Boundaries

- No Windows CRI support.
- Host must provide image availability, virtualization support, and the expected CRI tooling.
- The ignored `crictl` smoke suite must be run explicitly on a suitable host.
- Treat CNI/networking, multi-container edge cases, checkpointing, and full kubelet conformance as work in progress.
