---
title: "Skills"
description: "Load file-based and built-in skills"
---

# Skills

```ts
const session = agent.session('/repo', {
  builtinSkills: true,
  skillDirs: ['./skills'],
});
```

Example skill:

```md
---
name: release-review
description: Review release blockers
allowed-tools: "read(*), grep(*), bash(npm run build*)"
---

Return blockers first, with evidence.
```
