---
title: "AHP Safety"
description: "Attach an external harness to a session"
---

# AHP Safety

```ts
import { Agent, HttpTransport } from '@a3s-lab/code';

const agent = await Agent.create('agent.acl');
const session = agent.session('/repo', {
  ahpTransport: new HttpTransport('http://localhost:8080/ahp', process.env.AHP_TOKEN),
});
```

Use AHP for centralized policy, context injection, idle decisions, audit, and action supervision.
