---
title: Git Worktree
description: Parallel workspace isolation with the git builtin tool — create, list, remove, and status
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Callout } from 'fumadocs-ui/components/callout';

# Git Worktree

The `git` builtin tool provides Git operations including parallel workspace isolation via Git worktrees. Create feature branches in separate directories, work on them independently, and clean up when done — all without switching branches. Git auto-installs if not available.

<Callout type="info">
The `git` tool requires the session workspace to be inside a Git repository with at least one commit.
</Callout>

## Direct Tool Calls

<Tabs groupId="lang" items={['TypeScript', 'Python']}>
<Tab value="TypeScript">
```typescript
import { Agent } from '@a3s-lab/code';

const agent = await Agent.create('~/.a3s/config.hcl');
const session = agent.session('/my-git-repo');

// Check repository status
let r = await session.tool('git', { command: 'status' });
console.log(r.output);

// Create a worktree for a feature branch
r = await session.tool('git', {
  command: 'worktree',
  subcommand: 'create',
  name: 'feature-auth',
  path: '/my-git-repo/wt-feature-auth',
});
assert(r.exitCode === 0);

// List all worktrees
r = await session.tool('git', { command: 'worktree', subcommand: 'list' });
console.log(r.output); // Shows main + feature-auth

// Remove the worktree
r = await session.tool('git', {
  command: 'worktree',
  subcommand: 'remove',
  path: '/my-git-repo/wt-feature-auth',
});
assert(r.exitCode === 0);
```

**Run:** `node examples/test_git_worktree.ts`
**Source:** [`sdk/node/examples/test_git_worktree.ts`](https://github.com/A3S-Lab/Code/blob/main/sdk/node/examples/test_git_worktree.ts)
</Tab>
<Tab value="Python">
```python
from a3s_code import Agent

agent = Agent.create("~/.a3s/config.hcl")
session = agent.session("/my-git-repo")

# Check repository status
r = session.tool("git", {"command": "status"})
print(r.output)

# Create a worktree for a feature branch
r = session.tool("git", {
    "command": "worktree",
    "subcommand": "create",
    "name": "feature-auth",
    "path": "/my-git-repo/wt-feature-auth",
})
assert r.exit_code == 0

# List all worktrees
r = session.tool("git", {"command": "worktree", "subcommand": "list"})
print(r.output)  # Shows main + feature-auth

# Remove the worktree
r = session.tool("git", {
    "command": "worktree",
    "subcommand": "remove",
    path: "/my-git-repo/wt-feature-auth",
})
assert r.exit_code == 0
```

**Run:** `python examples/test_git_worktree.py`
**Source:** [`sdk/python/examples/test_git_worktree.py`](https://github.com/A3S-Lab/Code/blob/main/sdk/python/examples/test_git_worktree.py)
</Tab>
</Tabs>

## LLM-Driven Usage

The agent can use `git worktree` autonomously when asked to work on parallel features:

## Subcommands

<TypeTable
  type={{
    "status": {
      description: "Show current repository info (branch, commit, clean/dirty, worktree status)",
    },
    "log": {
      description: "Show commit log. Params: max_count (optional, default 10)",
    },
    "branch": {
      description: "List or create branches. Params: name (optional for create)",
    },
    "worktree list": {
      description: "List all worktrees with branch and path info",
    },
    "worktree create": {
      description: "Create a new worktree. Params: name (required), path (optional), new_branch (default true)",
    },
    "worktree remove": {
      description: "Remove a worktree by path. Params: path (required), force (optional)",
    },
  }}
/>

## API Reference

### git input schema (worktree commands)

<TypeTable
  type={{
    command: { type: 'string', required: true, description: '"worktree"' },
    subcommand: { type: 'string', required: true, description: '"list" | "create" | "remove"' },
    name: { type: 'string', description: 'Branch name (required for create)' },
    path: { type: 'string', description: 'Worktree directory path (required for create/remove)' },
    new_branch: { type: 'boolean', default: 'true', description: 'Create new branch for worktree' },
    base: { type: 'string', description: 'Base ref for new branch (default HEAD)' },
    force: { type: 'boolean', description: 'Force remove (default false)' },
  }}
/>
