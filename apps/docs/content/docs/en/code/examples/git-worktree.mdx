---
title: "Git Worktree"
description: "Use git through the session tool surface"
---

# Git Worktree

```ts
const status = await session.git('status');
const diff = await session.git('diff');
const log = await session.git('log', undefined, undefined, undefined, undefined, undefined, undefined, 5);
```

Git commands still pass through permission policy. Put push, publish, and release commands behind `ask` or `deny`.
