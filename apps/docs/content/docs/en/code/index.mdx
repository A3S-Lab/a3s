---
title: "A3S Code"
description: "Harness-driven coding agent runtime with Rust, Node.js, and Python SDKs"
---

# A3S Code

A3S Code is the harness-driven runtime behind `a3s code` and the language
SDKs. It keeps the agent loop observable while the harness owns context
assembly, tool execution, permission policy, delegation, workspace access,
persistence, verification evidence, and run replay.

## Current Capability Map

| Area | Current capability |
| --- | --- |
| Agent sessions | `Agent` / `AgentSession` expose `send`, `stream`, direct tools, run state, cancellation, persistence, and lifecycle cleanup. |
| Filesystem-first | [Filesystem-first](/docs/code/filesystem-first) organizes `AGENTS.md`, `agent.acl`, `.a3s/agents/`, `skills/`, `tools/`, and `schedules/` into a reviewable agent product interface. |
| Tools | Stable tool names cover files, search, shell, git, web fetch/search, batch execution, structured output, programmatic QuickJS tool chains, skills, MCP tools, and child-task delegation. |
| Delegation | Built-in roles and custom Markdown/YAML agents are available through `task`, `parallel_task`, `session.task(...)`, `session.tasks(...)`, and automatic delegation controls. |
| Orchestration | [`session.parallel`](/docs/code/orchestration), `session.pipeline`, workflow phases, resumable checkpoints, loop caps, and workflow budget ledgers provide deterministic host-driven fan-out. |
| Safety | Permission policies, HITL confirmation, workspace path checks, tool timeouts, sandbox handles, security providers, and prompt boundary injection all feed the same execution path. |
| Workspaces | [Workspace Backends](/docs/code/workspace-backends) let built-in tools target local files, host-provided workspaces, optional S3-compatible storage, and optional remote-git services. |
| Persistence | Memory/file stores, session IDs, auto-save, run snapshots/events, trace artifacts, memory recall, and retention caps support resumable sessions and replayable product state. |

The execution shape is:

```text
Agent / AgentSession
  -> context assembly
  -> optional planning
  -> selected tools or delegated child tasks
  -> permission and confirmation policy
  -> execution
  -> trace, artifacts, and verification evidence
  -> compaction and persistence
```

## Install

```bash
npm install @a3s-lab/code
pip install a3s-code
cargo add a3s-code-core
```

## Configure

A3S Code uses ACL. Keep real API keys, private model endpoints, local config
paths, and tenant/user identifiers out of commits. Commit templates that resolve
secrets from the environment.

```text
default_model = "provider/model-id"
max_parallel_tasks = 4
auto_parallel = false

providers "provider" {
  apiKey = env("PROVIDER_API_KEY")
  baseUrl = env("PROVIDER_BASE_URL")

  models "model-id" {
    tool_call = true
    limit = {
      context = 128000
      output = 4096
    }
  }
}

agent_dirs = ["./.a3s/agents"]
skill_dirs = ["./skills"]
storage_backend = "file"
```

`auto_parallel = false` disables automatic parallel child-agent fan-out only.
Manual `parallel_task` and SDK `session.tasks(...)` remain available unless
manual delegation is disabled separately.

## Use

```ts
import { Agent } from '@a3s-lab/code';

const agent = await Agent.create('agent.acl');
const session = agent.session('/path/to/workspace', {
  builtinSkills: true,
  planningMode: 'auto',
  permissionPolicy: {
    allow: ['read(*)', 'grep(*)', 'glob(*)'],
    ask: ['bash(*)', 'write(*)'],
    deny: ['write(**/.env*)', 'bash(rm -rf*)'],
    defaultDecision: 'ask',
    enabled: true,
  },
});

const result = await session.send('Find the authentication entry points.');
console.log(result.text);
console.log(result.verificationSummaryText);
```

## Entry Points

- [API Contract](/docs/code/api-contract) documents the integration-tested Node SDK surface.
- [Sessions](/docs/code/sessions) covers lifecycle, streaming, run state, persistence, and cancellation.
- [Tools](/docs/code/tools) covers direct tools, typed tool errors, structured output, and QuickJS programs.
- [Tasks](/docs/code/tasks) covers model-driven child-task delegation.
- [Orchestration](/docs/code/orchestration) covers host-driven fan-out, pipelines, and resumable workflows.
- [Security](/docs/code/security) covers permissions, confirmations, hooks, and verification gates.
- [Providers](/docs/code/providers) covers ACL provider configuration and environment injection.
