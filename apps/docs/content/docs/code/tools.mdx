---
title: Built-in Tools
description: 11 built-in tools for file operations, shell execution, and web access
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

# Built-in Tools

A3S Code ships with 11 built-in tools that cover file operations, shell execution, search, and web access. Tools are available in both native (library) and gRPC (server) modes.

## Tool Reference

| Tool | Purpose | Example |
|------|---------|---------|
| `bash` | Execute shell commands | `git status`, `npm install` |
| `read` | Read files with line numbers | View source code |
| `write` | Create/overwrite files | Create new files |
| `edit` | String replacement editing | Modify existing code |
| `patch` | Apply unified diff patches | Complex multi-line edits |
| `grep` | Search file contents (ripgrep) | Find function definitions |
| `glob` | Find files by pattern | `**/*.ts`, `src/**/*.rs` |
| `ls` | List directory contents | Explore project structure |
| `web_fetch` | Fetch web content | Download documentation |
| `web_search` | Search the web | Query multiple search engines |
| `cron` | Manage scheduled tasks | Create/list/run cron jobs |

## File Operations

### `read`

Read file contents with line numbers. Supports offset and limit for large files.

```json
{
  "path": "src/main.rs",
  "offset": 0,
  "limit": 100
}
```

### `write`

Create or overwrite a file. The tool automatically creates parent directories.

```json
{
  "path": "src/config.rs",
  "content": "pub struct Config {\n    pub port: u16,\n}\n"
}
```

### `edit`

Replace a specific string in a file. Useful for targeted modifications.

```json
{
  "path": "src/main.rs",
  "old_string": "fn main() {",
  "new_string": "fn main() -> Result<()> {"
}
```

### `patch`

Apply a unified diff patch. Best for complex multi-line edits.

```json
{
  "path": "src/lib.rs",
  "diff": "--- a/src/lib.rs\n+++ b/src/lib.rs\n@@ -1,3 +1,4 @@\n+use anyhow::Result;\n use std::io;\n"
}
```

## Search

### `grep`

Search file contents using ripgrep patterns. Supports regex, globs, and context lines.

```json
{
  "pattern": "fn.*async",
  "path": "src/",
  "glob": "*.rs",
  "context": 2
}
```

### `glob`

Find files matching a glob pattern.

```json
{
  "pattern": "src/**/*.rs"
}
```

### `ls`

List directory contents with file sizes and types.

```json
{
  "path": "src/"
}
```

## Shell

### `bash`

Execute shell commands. Commands run in the session's workspace directory.

```json
{
  "command": "cargo test --lib -- test_name"
}
```

## Web

### `web_fetch`

Fetch content from a URL. Returns text content extracted from HTML.

```json
{
  "url": "https://docs.rs/tokio/latest/tokio/"
}
```

### `web_search`

Search the web using multiple search engines.

```json
{
  "query": "rust async trait object",
  "engine": "google"
}
```

## Scheduling

### `cron`

Manage scheduled tasks with cron expressions or natural language.

```json
{
  "action": "create",
  "name": "backup",
  "schedule": "every day at 2am",
  "command": "backup.sh"
}
```

Supported natural language formats:
- English: `every 5 minutes`, `daily at 2am`, `every monday at 9:30`
- Chinese: `每5分钟`, `每天凌晨2点`, `每周一上午9点30分`

## Direct Tool Execution (Native)

In native mode, call tools directly without going through the LLM:

<Tabs groupId="lang" items={['Rust', 'Python', 'Node.js']}>
<Tab value="Rust">
```rust
// Convenience wrappers
let content = agent.read_file("src/main.rs").await?;
let output = agent.bash("cargo test --lib").await?;
let files = agent.glob("**/*.rs").await?;
let matches = agent.grep("fn main").await?;

// Generic tool call with JSON args
let result = agent.tool("edit", serde_json::json!({
    "path": "src/main.rs",
    "old_string": "fn main() {",
    "new_string": "fn main() -> Result<()> {"
})).await?;
println!("[{}] {}", result.exit_code, result.output);
```
</Tab>
<Tab value="Python">
```python
# Convenience wrappers
content = agent.read_file("src/main.rs")
output = agent.bash("cargo test --lib")
files = agent.glob("**/*.rs")
matches = agent.grep("fn main")

# Generic tool call with dict args
result = agent.tool("edit", {
    "path": "src/main.rs",
    "old_string": "fn main() {",
    "new_string": "fn main() -> Result<()> {"
})
print(f"[{result['exit_code']}] {result['output']}")
```
</Tab>
<Tab value="Node.js">
```typescript
// Convenience wrappers
const content = await agent.readFile('src/main.rs');
const output = await agent.bash('cargo test --lib');
const files = await agent.glob('**/*.rs');
const matches = await agent.grep('fn main');

// Generic tool call
const result = await agent.tool('edit', {
  path: 'src/main.rs',
  old_string: 'fn main() {',
  new_string: 'fn main() -> Result<()> {',
});
console.log(`[${result.exit_code}] ${result.output}`);
```
</Tab>
</Tabs>

## File Version History

All file-modifying tools (`write`, `edit`, `patch`) automatically capture a snapshot before making changes. You can:

- View the diff between versions
- Restore any previous version
- Track all modifications made during a session

## Extending with Custom Tools

Beyond built-in tools, A3S Code supports:

- [Skills](/docs/code/skills) — Markdown-based prompt extensions with tool permissions
- [MCP](/docs/code/mcp) — External tools via Model Context Protocol
- [LSP](/docs/code/lsp) — Code intelligence via Language Server Protocol
